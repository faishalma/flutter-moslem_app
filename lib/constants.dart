import 'package:flutter/material.dart';

const cBackgroundColor = Color(0xFFFFFFFF);

const cPrimaryColor = Color(0xFF22BB9F);

const cInactiveIconColor = Color(0xFFB3C0E0);

const cPrimaryTextColor = Color(0xFF000000);

const kDefaultPadding = 20;

const cShadowColor = Color(0x40000000);

const kDefaultShadow = BoxShadow(
  offset: Offset(0, 15),
  blurRadius: 27,
  color: Colors.black12, // Black color with 12% opacity
);
